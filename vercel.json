{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx prisma migrate deploy --schema=./prisma/schema.prisma && npm run build",
  "installCommand": "npm install",
  "devCommand": "npm run dev"
}